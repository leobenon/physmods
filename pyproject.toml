[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "physmods"
version = "0.1.0"
description = "Personal physics modules and simulations"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["Physics_modules"]
